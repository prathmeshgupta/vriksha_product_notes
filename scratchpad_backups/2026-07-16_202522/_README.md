# Scratchpad snapshot — 2026-07-16, 20:25 IST

Full copy of Claude's working scratchpad at the time this was taken. Most of this is superseded draft material from earlier build phases (pre-Supabase, pre-production-app), kept here only as a safety net in case something in here turns out to matter that wasn't obvious at the time.

**The authoritative, current files are NOT in here** — they're in the repo root: `index.html`, `ARCHITECTURE.md`, `ROADMAP.md`, `README.md`. Anything in this snapshot with the same name as a repo-root file is an OLDER, STALE copy — do not treat it as current.

## What's in this snapshot

- `portfolio_dashboard.html`, `portfolio_dashboard_v2.html`, `artifact_source.html`, `dashboard_shell.html` — early draft iterations of the app before the Supabase-backed production version existed.
- `app_logic_part1.js`–`app_logic_part5.js`, `products.js`, `extracted_script_scratch.js`, `extracted_v2.js` — intermediate code extraction/editing artifacts from mid-session work.
- `smoketest*.js` — ad-hoc verification scripts used during development.
- `portfolio_dashboard_production.html` — a working copy used during editing; the real, current version is `index.html` in the repo root.
- `ARCHITECTURE.md`, `ROADMAP.md`, `GIT_RECOVERY_STEPS.md` — stale/duplicate copies of documents that were correctly written to the repo root; the repo-root versions are current, these are not.
- `vriksha_repo/` — an early, incomplete draft of this repo from before it was properly set up.
- `scratch_check/`, `.sync_sentinel` — trivial debugging artifacts, no content of value.

This folder is a backup, not part of the app or its documentation — see `README.md` in the repo root for the actual working agreement on where files should live going forward.
